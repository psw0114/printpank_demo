import PBElement from '../../utils/pb_element.js';
import Utils from '../../utils/utils.js';

export default class Consulting extends PBElement {
  constructor() {
    super();
  }

  async init() { }

  render(){
    let html = '';

    this.innerHTML = html;
  }
}